import validateBody from './login.validate';
import validateName from './name.validate';
import validateAmount from './amount.validate';

export = {
  validateBody,
  validateName,
  validateAmount,
};