import validateBody from './login.validate';
import validateName from './name.validate';
import validateAmount from './amount.validate';
import validateUsername from './username.validate';
import validateClasse from './classe.validate';
import validateLevel from './level.validate';
import validatePassword from './password.validate';

export = {
  validateBody,
  validateName,
  validateAmount,
  validateUsername,
  validateClasse,
  validateLevel,
  validatePassword,
};